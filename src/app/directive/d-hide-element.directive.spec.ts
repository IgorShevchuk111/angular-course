import { DHideElementDirective } from './d-hide-element.directive';

describe('DHideElementDirective', () => {
  it('should create an instance', () => {
    const directive = new DHideElementDirective();
    expect(directive).toBeTruthy();
  });
});
